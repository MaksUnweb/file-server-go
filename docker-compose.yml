services:
  db:
    container_name: file_db 
    image: postgres:latest
    ports:
      - "5432:5432"
    volumes:
      - ./db:/var/lib/postgres
    environment:
      POSTGRES_USER: Admin 
      POSTGRES_PASSWORD: 1111
      POSTGRES_DB: file_db
